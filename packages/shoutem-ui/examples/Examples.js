import React from 'react';
import {
  ScrollView,
} from 'react-native';

import { Typography } from './Typography';

export function Examples() {
  return (
    <ScrollView>
      <Typography />
    </ScrollView>
  );
}
