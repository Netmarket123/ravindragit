import org.apache.tools.ant.taskdefs.condition.Os

exec {
  workingDir '../../'

  if (Os.isFamily(Os.FAMILY_WINDOWS)) {
      commandLine "cmd", "/c", "node", ".\\build-script\\index.js"
  } else {
      commandLine "./build-script/index.js"
  }
}